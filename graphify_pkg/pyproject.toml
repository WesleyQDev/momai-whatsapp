[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "graphify"
version = "0.1.0"
description = "Graphify wrapper for opencode tools"

[tool.setuptools.packages.find]
where = ["."]
include = ["graphify*"]
