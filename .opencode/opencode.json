{
  "$schema": "https://opencode.ai/config.json",
  "instructions": [".opencode/rules/**"],
  "model": "auto",
  "mcp": {
    "trello": {
      "type": "local",
      "command": ["bunx", "@delorenj/mcp-server-trello"],
      "enabled": true,
      "timeout": 30000,
      "environment": {
        "TRELLO_API_KEY": "${TRELLO_API_KEY}",
        "TRELLO_TOKEN": "${TRELLO_TOKEN}",
        "TRELLO_BOARD_ID": "${TRELLO_DEFAULT_BOARD_ID}"
      }
    }
  }
}
