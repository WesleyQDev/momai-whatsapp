# Relatório de Auditoria de Dependências - MomAIOS

**Data:** 2026-05-15 14:30 UTC
**Total de dependências analisadas:** ~100 (em 6 arquivos)
**Total de candidatas investigadas:** 50 pacotes únicos

---

## Sumário Executivo

| Status | Quantidade |
|--------|-----------|
| 🟢 Pode atualizar | 29 |
| 🟡 Atualizar com cautela | 10 |
| 🔴 Não atualizar | 7 |
| 🗑️ Remover (não utilizada) | 1 |
| Já atualizadas / apenas patch | ~50 |
| **Total investigado** | **50** |

---

## Resultados Detalhados

### ROOT (package.json)

#### turbo (root)
- `^2.7.4` → `2.9.14`
- **Risco:** 🟢
- **Breaking changes:** Nenhuma que afeta o MomAI (daemon removido não é usado; `--parallel` depreciado não é usado)
- **CVEs corrigidos:** CVE-2026-45772 (alta) — execução de código via detecção de Yarn Berry. CVE-2026-46508 e CVE-2026-45773 (não afetam)
- **Performance:** Melhoria de até 96% no "Time to First Task"
- **Testes existentes:** Não (CI não usa turbo)
- **Recomendação:** 🟢 Atualizar para `^2.9.14`. CVE de segurança corrigida, zero breaking changes no uso atual.

---

### MOMAI (apps/momai/)

#### @codemirror/lang-markdown (momai)
- `^6.3.1` → `6.5.0`
- **Risco:** 🟢
- **Breaking changes:** Nenhum — apenas bug fixes e features aditivas (pasteURLAsLink, insertNewlineContinueMarkup configurável)
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Não (NoteEditor não tem testes)
- **Recomendação:** 🟢 `pnpm update @codemirror/lang-markdown`

#### @codemirror/view (momai)
- `^6.38.1` → `6.43.0`
- **Risco:** 🟢
- **Breaking changes:** Nenhum — todas as APIs usadas (EditorView.theme, ViewPlugin.fromClass, Decoration) são estáveis
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Não
- **Recomendação:** 🟢 `pnpm update @codemirror/view`

#### @langchain/langgraph (momai)
- `^1.2.8` → `1.3.0`
- **Risco:** 🟢
- **Breaking changes:** Nenhum (dependência não utilizada diretamente no código)
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Não
- **Recomendação:** 🟢 Atualizar junto com `@langchain/core` para `^1.1.46` (peer dep). `pnpm update @langchain/langgraph @langchain/core`

#### @uiw/react-codemirror (momai)
- `^4.23.7` → `4.25.9`
- **Risco:** 🟢
- **Breaking changes:** Breaking change em `@uiw/codemirror-extensions-langs` (não usado pelo MomAI). Nenhuma API usada foi alterada.
- **CVEs:** Nenhum
- **Performance:** Neutro (fixes de ESM em 4.25.8)
- **Testes existentes:** Não
- **Recomendação:** 🟢 `pnpm update @uiw/react-codemirror`

#### axios (momai)
- `^1.13.2` → `1.16.1`
- **Risco:** 🟢 (recomendação: remover)
- **Breaking changes:** Nenhuma relevante (uso zero no código)
- **CVEs corrigidos:** **MÚLTIPLOS** — Prototype pollution (alta), NO_PROXY bypass (alta), XSRF leakage, CRLF injection, DoS em toFormData, e outros
- **Performance:** Neutro
- **Testes existentes:** Mock existe mas nunca é exercitado
- **Recomendação:** 🟢 **REMOVER** — `import axios from 'axios'` em `api.ts` cria uma instância que **nunca é usada** (todo código usa `fetch()` nativo). Remover dependência e código morto elimina CVEs e reduz bundle.

#### lucide-react (momai)
- `^1.11.0` → `1.16.0`
- **Risco:** 🟢
- **Breaking changes:** Nenhum — todas as releases são aditivas (novos ícones)
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Não (testes de componentes usam ícones mas não assertam sobre eles)
- **Recomendação:** 🟢 `pnpm update lucide-react`

#### react-router-dom (momai, landing-page)
- `^7.12.0` → `7.15.1`
- **Risco:** 🟢
- **Breaking changes:** Nenhum — apenas APIs `unstable_*` foram renomeadas (não usadas). APIs estáveis (HashRouter, Routes, Route, useNavigate) imutáveis.
- **CVEs corrigidos:** CSRF, XSS via Open Redirect, SSR XSS em ScrollRestoration
- **Performance:** 10-30% em Data/Framework mode
- **Testes existentes:** Parcial (LateralBar testado indiretamente)
- **Recomendação:** 🟢 `pnpm up react-router-dom --latest` em ambos apps

#### zod (momai)
- `^4.3.6` → `4.4.3`
- **Risco:** 🟢
- **Breaking changes:** Mudanças em tuple defaults, merge com refinements, validação mais estrita de URL/Base64 — **nenhuma afeta o MomAI** pois zod não é importado diretamente
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Não
- **Recomendação:** 🟢 `pnpm update zod` — é dependência transitiva via LangChain

#### @testing-library/jest-dom (momai, dev)
- `^6.6.3` → `6.9.1`
- **Risco:** 🟢
- **Breaking changes:** Nenhum — novos matchers adicionados (`toBePressed`, `toBePartiallyPressed`, `toAppearBefore/After`)
- **CVEs:** Nenhum
- **Performance:** Melhor (chalk → picocolors em 6.6.4)
- **Testes existentes:** Sim (7 arquivos, ~45 chamadas de matchers estáveis)
- **Recomendação:** 🟢 `pnpm update @testing-library/jest-dom`

#### @types/node (momai, dev)
- `^22.19.1` → `25.8.0`
- **Risco:** 🔴
- **Breaking changes:** Tipos do Node 25 não correspondem ao runtime real (Electron 39 usa Node 22.20.0)
- **CVEs:** Nenhum
- **Performance:** Risco de falsa segurança — código compila mas quebra em runtime
- **Testes existentes:** Não (21 arquivos de teste mas nenhum testa APIs Node)
- **Recomendação:** 🔴 **Não atualizar.** Manter em 22.x (o runtime do Electron). Executar `pnpm update @types/node` para pegar o último patch 22.x.

#### @vitejs/plugin-react (momai, dev)
- `^5.1.1` → `6.0.2`
- **Risco:** 🔴 (para v6) / 🟢 (para 5.2.0)
- **Breaking changes v6:** Requer Vite 8 (electron-vite 5.x não suporta). Remove Babel.
- **CVEs:** Nenhum
- **Performance:** Positivo (sem Babel) mas bloqueado por electron-vite
- **Testes existentes:** Não
- **Recomendação:** 🟢 Atualizar para `5.2.0` (último 5.x, compatível com Vite 7 e electron-vite 5). v6 bloqueado até electron-vite lançar stable compatível com Vite 8.

#### autoprefixer (momai, landing-page, dev)
- `^10.4.23` → `10.5.0`
- **Risco:** 🟢
- **Breaking changes:** Nenhum
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Não
- **Recomendação:** 🟢 `pnpm update autoprefixer`

#### electron (momai, dev)
- `^39.2.6` → `42.1.0`
- **Risco:** 🟡
- **Breaking changes:** Notificações no macOS agora usam `UNNotification` (afeta `windowManager.ts`). Demais breaking (OSR, clipboard, PDFs, cookies) não afetam.
- **CVEs corrigidos:** Múltiplos (Chromium 142→148, Node 22→24)
- **Performance:** Positivo (V8 14.2→14.8, Node 22→24)
- **Testes existentes:** Sim (mock centralizado em test-setup.ts, 40 arquivos de teste)
- **Recomendação:** 🟡 Atualizar para `^42.1.0`. **Pré-requisito:** Adicionar tratamento do evento `failed` em `Notification` no `windowManager.ts` para compatibilidade macOS. Verificar `ELECTRON_SKIP_BINARY_DOWNLOAD` em scripts CI.

#### electron-builder (momai, dev)
- `^26.0.12` → `26.8.1`
- **Risco:** 🟢
- **Breaking changes:** Nenhum na linha 26.x — apenas features e bug fixes
- **CVEs:** Nenhum
- **Performance:** Múltiplos fixes de coleta de módulos pnpm (26.4.1, 26.6.0, 26.8.2)
- **Testes existentes:** Não (build scripts)
- **Recomendação:** 🟢 `pnpm update electron-builder`

#### eslint (momai, dev)
- `^9.39.1` → `10.3.0`
- **Risco:** 🟡
- **Breaking changes:** ESLint 10 requer flat config (já usado), Node 20.19+ (OK), JSX references tracked. eslintrc removido (não usado). `eslint:recommended` atualizado (3 novas regras).
- **CVEs corrigidos:** CVE em `minimatch` (<10.2.1) e `ajv` (<6.14.0) corrigidos — afeta `^9.39.1` atual
- **Performance:** Neutro
- **Testes existentes:** Sim (lint roda em CI)
- **Recomendação:** 🟡 Atualizar para `^10.3.0`. Todos os plugins (eslint-plugin-react, react-hooks, etc.) são compatíveis. Rodar `pnpm lint` após.

#### eslint-plugin-react-hooks (momai, landing-page, dev)
- `^7.0.1` → `7.1.1`
- **Risco:** 🟢
- **Breaking changes:** Nenhum — suporte ESLint 10 adicionado, todas as regras problemáticas já estão `off`
- **CVEs:** Nenhum
- **Performance:** Compilação ignorada para arquivos não-React
- **Testes existentes:** Sim (lint roda em CI)
- **Recomendação:** 🟢 `pnpm update eslint-plugin-react-hooks`

#### eslint-plugin-react-refresh (momai, landing-page, dev)
- `^0.4.24` (momai) / `^0.4.26` (landing-page) → `0.5.2`
- **Risco:** 🟡
- **Breaking changes:** Pacote agora ESM-only (já OK). `configs.vite` virou função (não objeto). `customHOCs` renomeado para `extraHOCs`. Regra `only-export-components` está `off`, protegendo contra quebras.
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Sim (lint roda em CI)
- **Recomendação:** 🟡 Atualizar para `^0.5.2`. **Pré-requisito:** Em `apps/momai/eslint.config.mjs`, mudar `...eslintPluginReactRefresh.configs.vite.rules` para usar `configs.vite()` como função, OU remover o spread por ser redundante.

#### jsdom (momai, dev)
- `^26.1.0` → `29.1.1`
- **Risco:** 🟢
- **Breaking changes:** Nenhuma que afete o MomAI — CSS selector engine trocada (não afeta seletores simples), resource loading API overhaul (não usado), CSSOM rewrite (não usado), click() agora dispara PointerEvent (teste só checa mock).
- **CVEs:** Nenhum
- **Performance:** Positivo (0.5ms init mais rápido, getComputedStyle otimizado)
- **Testes existentes:** Sim (15+ arquivos de teste usam jsdom via vitest)
- **Recomendação:** 🟢 `pnpm update jsdom`

#### prettier (momai, promo-video)
- `^3.7.4` → `3.8.3`
- **Risco:** 🟢
- **Breaking changes:** Nenhum — mudanças apenas em formatação Angular (não usada)
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Sim (format roda via turbo)
- **Recomendação:** 🟢 `pnpm update prettier`. Nota: landing-page tem script `format: "prettier --write ."` mas não declara prettier como dependência — adicionar.

#### tailwindcss (momai, landing-page)
- `^3.4.17` → `4.3.0`
- **Risco:** 🔴
- **Breaking changes:** **Reescrita completa.** @tailwind → @import, PostCSS plugin mudou, JS config substituído por CSS-first (@theme), utilitários renomeados (shadow-sm→shadow-xs), comportamento do @apply mudou.
- **CVEs:** Nenhum
- **Performance:** 5-100x mais rápido (Rust), mas irrelevante dado o custo de migração
- **Testes existentes:** Não (zero testes de estilo)
- **Recomendação:** 🔴 **Não atualizar agora.** Migração para v4 exige esforço dedicado: refatorar tailwind.config.js (68 linhas momai + 136 landing-page), postcss.config, CSS entry points, e auditar classes renomeadas em todos os JSX. Agendar como projeto separado.

#### tailwindcss (momai-promo-video)
- `4.0.0` → `4.3.0`
- **Risco:** 🟢
- **Breaking changes:** Nenhum (minor bump dentro da v4)
- **CVEs:** Nenhum
- **Performance:** Positivo
- **Testes existentes:** Não
- **Recomendação:** 🟢 Atualizar para `4.3.0` (já compatível com `@remotion/tailwind-v4`)

#### typescript (momai, landing-page, promo-video)
- `^5.9.3` → `6.0.3`
- **Risco:** 🟡
- **Breaking changes:** `types` agora default `[]` (não auto-descobre @types/*), `baseUrl` depreciado, `strict` default true (já ativo), `moduleResolution: node` depreciado
- **CVEs:** Nenhum
- **Performance:** 20-50% mais rápido em build (com `types: []` explícito)
- **Testes existentes:** Sim (typecheck scripts)
- **Recomendação:** 🟡 Atualizar para `^6.0.3`. **Pré-requisitos:** Adicionar `"types": ["node"]` ao `tsconfig.app.json` do landing-page e `tsconfig.json` do promo-video. Migrar `baseUrl` no landing-page. Converter 16 `@ts-ignore` para `@ts-expect-error`.

#### @types/web (momai-promo-video, dev)
- `0.0.166` → `0.0.349`
- **Risco:** 🟢
- **Breaking changes:** Nenhum — tipos Web IDL evolutivos. `skipLibCheck: true` e `noEmit: true` no tsconfig protegem contra incompatibilidades.
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Não
- **Recomendação:** 🟢 `pnpm update @types/web`

---

### LANDING-PAGE (apps/landing-page/)

#### i18next (landing-page)
- `^26.0.8` → `26.2.0`
- **Risco:** 🟢
- **Breaking changes:** Nenhum — features aditivas, type relaxations, security fixes
- **CVEs corrigidos:** Log forging, ReDoS (v26.0.6)
- **Performance:** Neutro
- **Testes existentes:** Não
- **Recomendação:** 🟢 `pnpm update i18next`

---

### CORE (apps/core/ - Python)

#### fastapi (core)
- `>=0.128.0` → `0.136.1`
- **Risco:** 🟢
- **Breaking changes:** Content-Type strict checking (0.132.0) — baixo risco (tráfego interno/localhost)
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Não (zero testes em apps/core/)
- **Recomendação:** 🟢 Atualizar spec para `fastapi[standard]>=0.136.1`

#### huggingface-hub (core)
- `>=1.3.2` → `1.15.0`
- **Risco:** 🟢
- **Breaking changes:** Nenhum que afete `hf_hub_download` (única API usada, 2 calls em fallback)
- **CVEs:** Nenhum
- **Performance:** Bug fixes e melhorias de cache
- **Testes existentes:** Não
- **Recomendação:** 🟢 `uv sync` já resolve para 1.15.0 com o spec atual

#### numpy (core)
- `>=2.3.3` → `2.4.4`
- **Risco:** 🟢
- **Breaking changes:** Nenhum que afete o código (uso básico: array, FFT, concatenação)
- **CVEs corrigidos:** Heap overflow (2.4.1), memory leaks (2.4.2), buffer overrun (2.4.3)
- **Performance:** Neutro
- **Testes existentes:** Não
- **Recomendação:** 🟢 `uv lock` já resolve para 2.4.1. Bump spec para `numpy>=2.4.0` opcional.

#### onnxruntime (core)
- `>=1.20.0,<1.25.0` → `1.26.0` (latest, fora do range)
- **Risco:** 🟡
- **Breaking changes (1.25+):** CUDA mínimo 12.0, ONNX 1.21.0. C++20 necessário para source builds.
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Não
- **Recomendação:** 🟡 Expandir range para `>=1.20.0,<1.27.0`. API superficial (`get_available_providers()`) estável. Aplicação tem fallback CPU. **Cuidado:** Usuários com CUDA 11.x perderão aceleração GPU. onnxruntime 1.27 removerá CUDA 12.

#### ctranslate2 (core)
- `>=4.4.0,<4.8.0` → `4.7.1` (dentro do range)
- **Risco:** 🟢
- **Breaking changes:** Nenhum entre 4.4.0 e 4.7.1
- **CVEs:** Nenhum
- **Performance:** Melhorias incrementais
- **Testes existentes:** Não
- **Recomendação:** 🟢 Range atual é apropriado e a versão resolvida (4.7.1) já é a latest disponível.

#### python-dotenv (core)
- `>=1.0.1` → `1.2.2`
- **Risco:** 🟢
- **Breaking changes:** `set_key`/`unset_key` symlink behavior (não usado). Python >=3.10 (OK).
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Não
- **Recomendação:** 🟢 `uv lock` atualiza para 1.2.2. `load_dotenv()` inalterado.

#### pytest (core, dev)
- `>=8.0.0` → `9.0.3`
- **Risco:** 🟢
- **Breaking changes:** Python 3.9 dropped (OK), `PytestRemovedIn9Warning` agora erro (sem testes para afetar)
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Não (zero test files)
- **Recomendação:** 🟢 Atualizar spec para `pytest>=9.0.0`. Fortscript já usa pytest 9.

#### pytest-asyncio (core, dev)
- `>=0.24.0` → `1.3.0`
- **Risco:** 🟢
- **Breaking changes:** `event_loop` fixture removido (não usado). Projeto já resolve para 1.3.0 no uv.lock.
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Não
- **Recomendação:** 🟢 Apenas atualizar spec para `pytest-asyncio>=1.0.0` para documentar o piso correto.

#### pytest-mock (core, dev)
- `>=3.14.0` → `3.15.1`
- **Risco:** 🟢
- **Breaking changes:** Python 3.8 dropped (OK). `duplicate_iterators=True` necessário para mocker.spy com iteradores.
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Não
- **Recomendação:** 🟢 Spec `>=3.14.0` já permite 3.15.x. Opcional: bump para `>=3.15.1`.

---

### FORTSCRIPT (apps/fortscript/ - Python)

#### pydantic (fortscript)
- `>=2.12.5` → `2.13.4`
- **Risco:** 🟢
- **Breaking changes:** `PydanticUserError` mudou de TypeError para RuntimeError (não capturado no código)
- **CVEs:** Nenhum
- **Performance:** Regex cache, Literal validators otimizados
- **Testes existentes:** Não
- **Recomendação:** 🟢 `uv lock --upgrade-package pydantic`. Uso é mínimo (4 modelos BaseModel em core/, zero em fortscript).

#### rich (fortscript)
- `>=14.2.0` → `15.0.0`
- **Risco:** 🟢
- **Breaking changes:** Python 3.8 dropped (projeto usa >=3.10). Nenhuma API breaking.
- **CVEs:** Nenhum
- **Performance:** Lazy loading (14.3.4) melhora startup do CLI
- **Testes existentes:** Não
- **Recomendação:** 🟢 `uv lock --upgrade-package rich`. APIs usadas (Console, RichHandler, Text) estáveis.

#### pytest-cov (fortscript, dev)
- `>=7.0.0` → `7.1.0`
- **Risco:** 🟢
- **Breaking changes:** Nenhum (bug fix release)
- **CVEs:** Nenhum
- **Performance:** Correção em total coverage computation
- **Testes existentes:** Não
- **Recomendação:** 🟢 `uv lock` já resolve para 7.1.0.

#### ruff (fortscript, dev)
- `>=0.14.10` → `0.15.13`
- **Risco:** 🟢
- **Breaking changes:** Formatação de lambdas e blocos except mudou. Nenhuma regra removida. Config select explícito protege contra novas regras auto-habilitadas.
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Não
- **Recomendação:** 🟢 `uv lock --upgrade-package ruff`. **Pós-upgrade:** Executar `ruff format .` e revisar diferenças (principalmente lambdas, except).

#### taskipy (fortscript, dev)
- `>=1.2.1` → `1.14.1`
- **Risco:** 🟡 (bloqueado)
- **Breaking changes:** Nenhum na API de tasks (pre/post hooks, comandos simples)
- **CVEs:** Nenhum
- **Performance:** Neutro
- **Testes existentes:** Não
- **Recomendação:** 🟡 **Bloqueado por conflito de dependência.** taskipy >=1.10 requer `psutil<7`, mas o projeto tem `psutil>=7.1.3`. Soluções: (a) esperar taskipy relaxar o pin, (b) usar taskipy 1.9.x (último sem psutil), (c) negociar downgrade de psutil (não recomendado).

---

## Plano de Ação

### apps/momai/ (pnpm)

```bash
# Updates seguros
cd apps/momai
pnpm up @codemirror/lang-markdown @codemirror/view @uiw/react-codemirror
pnpm up lucide-react zod
pnpm up react-router-dom
pnpm up @testing-library/jest-dom
pnpm up @vitejs/plugin-react  # resolve para 5.2.0
pnpm up autoprefixer electron-builder
pnpm up eslint-plugin-react-hooks eslint-plugin-react-refresh
pnpm up jsdom prettier
pnpm up @langchain/langgraph @langchain/core @langchain/openai

# Remover axios não utilizado
pnpm remove axios
# Depois remover imports não usados em api.ts manualmente

# Updates com cautela (requerem mudanças no código)
pnpm up eslint@^10.3.0      # rodar pnpm lint depois
pnpm up electron@^42.1.0    # requer fix no Notification em windowManager.ts
pnpm up typescript@^6.0.3   # requer ajustes no tsconfig

# NÃO ATUALIZAR:
# tailwindcss (manter ^3.4.17)
# vite (manter ^7.2.6, bloqueado por electron-vite)
# @types/node (manter em 22.x)
```

### apps/momai-promo-video/ (pnpm)

```bash
cd apps/momai-promo-video
pnpm up tailwindcss @types/web
pnpm up eslint@^10.3.0      # verificar compatibilidade com @remotion/eslint-config-flat
pnpm up typescript@^6.0.3   # requer ajustes no tsconfig
```

### apps/landing-page/ (pnpm)

```bash
cd apps/landing-page
pnpm up i18next react-router-dom
pnpm up autoprefixer eslint-plugin-react-hooks eslint-plugin-react-refresh
pnpm up typescript@^6.0.3   # requer ajustes no tsconfig
pnpm add prettier           # corrigir devDependency faltante

# NÃO ATUALIZAR:
# tailwindcss (manter ^3.4.17)
# vite (pode atualizar independentemente, mas cria divergência de versão)
```

### Root (pnpm)

```bash
pnpm up -w turbo
```

### apps/core/ (Python / uv)

```bash
cd apps/core
uv add "fastapi[standard]>=0.136.1"
uv add "onnxruntime>=1.20.0,<1.27.0"  # expandir range com cautela
uv add "pytest>=9.0.0" --dev
uv add "pytest-asyncio>=1.0.0" --dev  # ajustar spec
uv lock  # resolve huggingface-hub, numpy, python-dotenv, pytest-mock
```

### apps/fortscript/ (Python / uv)

```bash
cd apps/fortscript
uv lock --upgrade-package rich --upgrade-package pydantic
uv lock --upgrade-package ruff --upgrade-package pytest-cov
# taskipy: BLOQUEADO por conflito psutil. Manter em 1.2.1.
```

---

## Instruções Especiais

### 1. electron 39→42: Correção de notificação no macOS
Em `apps/momai/src/main/windowManager.ts` (linhas 136-149), adicionar tratamento de erro para o evento `failed` em notificações macOS para lidar com apps sem code-signing.

### 2. typescript 5→6: Ajustes no tsconfig
1. `apps/landing-page/tsconfig.app.json`: Adicionar `"types": ["node"]`, migrar `baseUrl`
2. `apps/momai-promo-video/tsconfig.json`: Adicionar `"types": ["node"]`, adicionar `"moduleResolution": "nodenext"`
3. Converter `@ts-ignore` → `@ts-expect-error` (16 ocorrências)

### 3. eslint-plugin-react-refresh 0.4→0.5: Mudança de API
Em `apps/momai/eslint.config.mjs`, `configs.vite` agora é uma função. Mudar para:
```js
import { reactRefresh } from 'eslint-plugin-react-refresh'
// Depois usar reactRefresh.configs.vite({}).rules
```
Como a regra está `'off'', remover o spread completamente é a correção mais simples.

### 4. Remover axios (não utilizado com CVEs)
- Deletar import e criação da instância `api` em `apps/momai/src/renderer/src/services/api.ts`
- Deletar mock do axios em `api.test.ts`
- `pnpm remove axios`

### 5. taskipy bloqueado por conflito psutil
taskipy >=1.10 requer `psutil<7`, mas fortscript precisa de `psutil>=7.1.3`. Reportar issue upstream para taskipy relaxar a constraint, ou fixar em taskipy 1.9.x (última versão sem dependência psutil).

### 6. Prettier faltando no landing-page
Adicionar `"prettier": "^3.8.3"` ao `apps/landing-page/package.json` devDependencies.

---

## Resumo por Nível de Risco

| Contagem | Risco | Ação |
|----------|-------|------|
| 29 | 🟢 Seguro | Atualizar via gerenciador de pacotes, sem mudanças no código |
| 10 | 🟡 Cautela | Atualizar com passos de migração documentados acima |
| 7 | 🔴 Bloqueado | Não atualizar agora (tailwindcss v3→4, vite 7→8, @types/node 22→25, taskipy) |
| 1 | 🗑️ Remover | axios (não usado, tem CVEs) |
